<h1>投稿詳細</h1>
<p><strong>タイトル:</strong> <%= @post.title %></p>
<p><strong>内容:</strong> <%= simple_format(@post.body) %></p>
<p><strong>作成者:</strong> <%= @post.user.name %></p>
<p><strong>作成日時:</strong> <%= @post.created_at.strftime("%Y-%m-%d %H:%M") %></p>
<div>
  <%= button_to "削除", admin_post_path(@post), method: :delete, class: "btn btn-danger", data: { confirm: "本当に削除しますか？" } %>
</div>
<%= link_to "投稿一覧に戻る", admin_posts_path, class: "btn btn-secondary mt-3" %>