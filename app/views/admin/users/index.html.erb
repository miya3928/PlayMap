<h1 class="mb-4 text-white text-center">ユーザー管理</h1>

<div class="table-responsive">
  <table class="table table-hover" style="background-color: rgba(255, 255, 255, 0.8); border-radius: 8px;">
    <thead style="background-color: rgba(0, 0, 0, 0.7); color: white;">
      <tr>
        <th>ID</th>
        <th>名前</th>
        <th>メールアドレス</th>
        <th>ステータス</th>
        <th>アクション</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td>
            <% if user.is_active %>
              <span class="badge bg-success">Active</span>
            <% else %>
              <span class="badge bg-secondary">Inactive</span>
            <% end %>
          </td>
          <td>
            <%= link_to "詳細", admin_user_path(user), class: "btn btn-info btn-sm" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>