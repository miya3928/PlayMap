<%= form_with url: search_posts_path, method: :get, local: true, class: "search-form d-flex" do |f| %>
  <%= f.text_field :keyword, placeholder: "キーワードを入力", class: "form-control me-2" %>
  <%= f.submit "検索", class: "btn btn-outline-success" %>
<% end %>