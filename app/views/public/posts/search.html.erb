<h1>検索結果</h1>
<div class="search-results">
  <% if @posts.any? %>
    <ul class="post-list">
      <% @posts.each do |post| %>
        <li class="post-item">
          <h2 class="post-title">
            <%= link_to post.title, post_path(post), class: "post-link" %>
          </h2>
          <p class="post-snippet"><%= truncate(post.body, length: 100) %></p>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="no-results-message">該当する投稿がありません。</p>
  <% end %>
</div>