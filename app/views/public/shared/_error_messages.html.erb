<% if resource.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <h4><%= pluralize(resource.errors.count, "件のエラー") %> が発生しました：</h4>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>